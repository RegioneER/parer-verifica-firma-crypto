spec:
  template:
    metadata:
      labels:
         ambiente: pre
    spec:
      volumes:
      - name: crypto-config
        configMap:
          name: crypto-config-pre
      containers:
      - env:
        - name: SPRING_CONFIG_VOLUME
          value: /etc/crypto/config
        - name: H2_USER
          valueFrom:
            secretKeyRef:
              key: database-user
              name: crypto-secrets-pre
        - name: H2_PWD
          valueFrom:
            secretKeyRef:
              key: database-password
              name: crypto-secrets-pre
        - name: ADMIN_USER
          valueFrom:
            secretKeyRef:
              key: admin-user
              name: crypto-secrets-pre
        - name: ADMIN_PWD
          valueFrom:
            secretKeyRef:
              key: admin-password
              name: crypto-secrets-pre
        volumeMounts:
        - name: crypto-config
          readOnly: true
          mountPath: /etc/crypto/config
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 180
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
